name: MCBot # the plugins name as it should appear in the plugin list /pl
version: 1.0 # the plugin's version
author: Uranium_Ape
main: uraniumape.mcbot.MCBot
api-version: 1.20
commands:
  mcbot:
    description: Admin commands for MCBot
    usage: /<command>
    permission: mcbot.admin